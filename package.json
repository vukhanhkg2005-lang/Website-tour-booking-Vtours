{
  "name": "tour-booking-system",
  "version": "1.0.0",
  "description": "Root package for running both backend and UI",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:ui\"",
    "start:backend": "cd backend && npm run dev",
    "start:ui": "cd ui && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../ui && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
